# GitHub safekeeping snapshot

The remote repository is a frozen source-only recovery snapshot of the former
Atom generative-English distillation work and the Atom-native
application-builder work from `C:\Projects\atom lora`.

Canonical GitHub location:

```text
https://github.com/Lucerna-Labs/atom-lora
```

The dedicated repository keeps this research separate from the bulky working
`Lucerna-Labs/atom-vibe-coder` application.

Included:

- causal-language and context-training source;
- chat admission, wiki graph, graph RAG, and side-view source;
- Atom-native specification, Rust target, and Svelte/TypeScript frontend
  generation source;
- Kaggle foundation, continuation, dialogue, context, and evaluation runners;
- focused integration tests, architecture contracts, operator notes, and
  compact hash/counter evidence.

Intentionally excluded:

- model weights and optimizer checkpoints;
- generated build trees, dependency caches, and runtime databases;
- large Kaggle result payloads, notebook downloads, backups, and logs;
- credentials, tokens, local environment files, and machine-specific secrets.

The generative-English distillation track was subsequently retired locally.
The GitHub snapshot must not be updated, rewritten, or deleted unless the
operator explicitly authorizes that specific backup mutation in a current
message.

Restore the source by checking out this branch. Restore trained weights
separately from the private Kaggle notebook outputs after verifying their
declared SHA-256 manifest.
